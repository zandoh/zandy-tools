# tonk notification personality — https://github.com/zandoh/tonk
# Posts use tonk's brand defaults (username "tonk" + bell icon) as of v2.1.0.
notify:
  failure: true
  recovery: true
  pull-requests: true # PR failures ping on purpose in this repo
